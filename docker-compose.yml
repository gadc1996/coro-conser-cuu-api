version: '3.8'

services:
  app:
    context: .
    build: .
    dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8080:8080"
    env_file:
      - .env.dev